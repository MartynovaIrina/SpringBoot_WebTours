server.port=8081
management.endpoints.web.exposure.include=health,info,jolokia,shutdown
#No auth  protected
endpoints.shutdown.sensitive=false

#Enable shutdown endpoint
endpoints.shutdown.enabled=true